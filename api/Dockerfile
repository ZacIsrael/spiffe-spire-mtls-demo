# Uses a lightweight Linux image for the temporary API workload container.
FROM alpine:3.22

# Keeps the container running so SPIRE can perform workload attestation against it.
CMD ["sleep", "infinity"]