# Uses the SPIRE Agent image so the workload can directly test the Workload API.
FROM ghcr.io/spiffe/spire-agent:1.11.2

# Keeps the container running so SPIRE can perform workload attestation against it.
CMD ["sleep", "infinity"]